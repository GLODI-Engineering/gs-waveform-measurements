# Claude Code project notes

Read [`AGENTS.md`](AGENTS.md) first. Project skills live under `.claude/skills/`; open the
appropriate `SKILL.md` before a recurring workflow. Hooks are installed through `pre-commit`. If
a hook fails, fix the underlying issue and never bypass it.
